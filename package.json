{
  "private": true,
  "name": "niji-live",
  "version": "1.0.0",
  "description": "niji.live",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "localhost",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
